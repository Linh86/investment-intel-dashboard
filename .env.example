# Copy to .env.local for local development.

# Reserved for the future live LLM mode — nothing reads these yet; the demo
# requires no API keys.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Fixture mode (the default even when unset) keeps the demo offline.
DEMO_MODE=true

